{
  "name": "hw29-todoapp",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tailwindcss -i ./css/input.css -o ./dist/styles.css",
    "watch": "npx tailwindcss -i ./css/input.css -o ./dist/styles.css --watch",
    "start": "live-server --port=8080 --open=index.html"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "live-server": "^1.2.2",
    "tailwindcss": "^3.4.7"
  }
}
